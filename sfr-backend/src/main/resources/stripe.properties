# Stripe Webhook Configuration
stripe.webhook.endpoint.secret=${STRIPE_WEBHOOK_SECRET:whsec_test_}
stripe.api.secret.key=${STRIPE_SECRET_KEY:sk_test_}

# Stripe Public Key (for frontend)
stripe.api.public.key=${STRIPE_PUBLIC_KEY:pk_test_}

# Stripe Webhook Endpoint URL (for documentation)
stripe.webhook.endpoint.url=${STRIPE_WEBHOOK_URL:http://localhost:8080/api/payment/stripe/webhook}
